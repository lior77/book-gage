<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No action bar: the web app draws its own top bar, and two of them on a
         phone screen is one too many. -->
    <style name="Theme.Porto" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
