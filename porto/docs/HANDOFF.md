# פורטולנד — מסמך מסירה

**למי שממשיך מכאן: אדם, מודל שפה, או שניהם.**
מתאר את המצב ב-2026-09-14, גרסה **1.45.0**, commit `2470e0e` ואילך.
‏`portoland-latest.apk` שב-release כבר נושא אותו — נבדק בתוך הקובץ.

מסמך זה אינו מחליף את `porto/docs/ARCHITECTURE.md` — הוא **נקודת הכניסה
אליו**. אדריכלות, מקורות ומודל הנתונים נמצאים שם ורק שם. כאן נמצא מה שאינו
שם: המצב הנוכחי, מה שפתוח, איך עובדים בפועל, ומה כבר עלה בזמן.

---

## ★ איפה הפרויקט

**המאגר:** `lior77/book-gage` — ‏https://github.com/lior77/book-gage
**ענף העבודה:** `claude/mobile-app-pdf-knowledge-aoomsp`

| מה | נתיב |
|---|---|
| שורש המאגר | `/home/user/book-gage` |
| **תיקיית הפרויקט — כאן כל העבודה** | **`/home/user/book-gage/porto`** |
| מסמך האדריכלות | `/home/user/book-gage/porto/docs/ARCHITECTURE.md` |
| חוזה הדיוק | `/home/user/book-gage/CLAUDE.md` |
| המסמך הזה | `/home/user/book-gage/porto/docs/HANDOFF.md` |
| ‏workflow ה-APK | `/home/user/book-gage/.github/workflows/porto-android-apk.yml` |

**הנתיב המוחלט נכון לסביבה שבה נכתב המסמך.** ‏`/home/user/book-gage` הוא
מקום השכפול במכולה המרוחקת; במחשב אחר הוא יהיה אחר. אם אינך יודע איפה אתה:

```bash
git rev-parse --show-toplevel        # שורש המאגר, מכל תיקייה בתוכו
```

**נוח לקבע את זה פעם אחת בתחילת סשן:**

```bash
export REPO="$(git rev-parse --show-toplevel)"
export PORTO="$REPO/porto"
cd "$PORTO"
```

כל הפקודות במסמך מניחות `cd "$PORTO"`.

> **מוסכמת נתיבים במסמך הזה:** נתיב שמתחיל ב-`porto/` הוא **יחסית לשורש
> המאגר**; נתיב בלי הקידומת (למשל `scripts/checks.py`) הוא **יחסית לתיקיית
> הפרויקט**, כלומר `$PORTO`. ההבחנה אינה קוסמטית: בשורש המאגר יש `index.html`
> משלו, ו-`porto/index.html` הוא קובץ אחר לגמרי.

**‏`porto/` היא תיקיית העבודה היחידה.** מה שיש בשורש המאגר מלבדה:

| | |
|---|---|
| `CLAUDE.md` | חוזה הדיוק. **חל על `porto/`** |
| `.github/workflows/` | ‏workflow בניית ה-APK |
| `porto_pdm_claude/` | **‏PDM של מחוז פורטו — כן קשור.** מחקר מקורות על Maia, Gondomar (וקטור מאומת), Gaia, Felgueiras (לא נפתרו). משלים את `GPT/PDM_RAW` שב-SharePoint. ראה §8 |
| `index.html`, `thankyou.html`, `background*.jpg`, `favicon.png`, `staticwebapp.config.json` | אתר סטטי נפרד בשורש. **לא חלק מהאפליקציה** |

טעות קלה לעשות: לערוך את `index.html` שבשורש במקום את `porto/index.html`.

‏`porto_pdm_claude/` נראה כמו פרויקט זר לפי השם והוא אינו — הוא **החצי השני**
של עבודת ה-PDM. אין לו קשר ללולאת הבנייה, ואין למחוק אותו.

---

## 0. סדר הקריאה, בפעם הראשונה

| # | קובץ | למה |
|---|---|---|
| 1 | `CLAUDE.md` (בשורש המאגר) | חוזה הדיוק. שבעה כללים שאינם ניתנים למשא ומתן |
| 2 | `porto/docs/ARCHITECTURE.md` | **מקור האמת.** מבנה, מקורות, מודל נתונים, מנגנוני אימות |
| 3 | הקובץ הזה | מצב, עבודה פתוחה, נוהג עבודה, מלכודות סביבה |
| 4 | ‏`porto/docs/ARCHITECTURE.md` §11 | **87 מלכודות ידועות.** כל אחת נכתבה אחרי שמשהו נשבר באמת |

**אל תתחיל לכתוב קוד לפני 1 ו-2.** רוב הטעויות בפרויקט הזה לא היו טעויות
תכנות — הן היו הנחות על הנתונים.

---

## 1. מה זו האפליקציה, בפסקה

**פורטולנד** הוא אטלס עברי, ‏RTL, של מחוז פורטו בפורטוגל, שנבנה עבור חיפוש
נכסים. ‏18 עיריות, ‏275 רובעים (חלוקת 2025), ‏1,770 יישובים ושכונות,
‏1,531 אתרים ומוסדות. **עובד בלי רשת**, בלי חשבון, בלי אנליטיקס, בלי שרת.
מתפרסם כ-PWA וכ-APK לאנדרואיד (‏WebView עוטף את אותו דף בדיוק).

שלוש רמות: מחוז ← עירייה ← רובע. מסך מחולק לשניים (מפה + טקסט) שעוקב אחרי
אחיזת הטלפון.

---

## 2. איפה הכול נמצא

```
/home/user/book-gage/                     ← $REPO — שורש המאגר
├── CLAUDE.md                             ← חוזה הדיוק. קרא ראשון
├── .github/workflows/
│   └── porto-android-apk.yml             ← בונה APK. רק מענף העבודה
└── porto/                                ← $PORTO ★ כל העבודה כאן
    ├── VERSION                           ← מקור האמת היחיד לגרסה
    ├── index.html                        ← 110 שורות. שלד בלבד
    ├── app.js                            ← 6,434 שורות. כל לוגיקת הלקוח
    ├── app.css                           ← 853 שורות
    ├── sw.js                             ← Service Worker — עבודה בלי רשת
    ├── manifest.webmanifest              ← הגדרות PWA
    ├── porto-standalone.html             ← תוצר של bundle_standalone.py
    ├── vendor/                           ← Leaflet 1.9.4, מקומי. לא CDN
    ├── icons/
    ├── data/
    │   ├── sources.json                  ← ★ מקור לכל שדה + מה חסר ולמה
    │   ├── prose_en.json                 ← הפרוזה באנגלית
    │   ├── layers_manifest.json
    │   ├── source_files/   (84M)         ← ארכיון. לא נוגעים
    │   ├── raw/            (94M)         ← קלט מנורמל
    │   ├── processed/     (1.6M)         ← ★ נוצר. לא עורכים ביד
    │   └── layers/         (22M)         ← שכבות להורדה לפי דרישה
    ├── scripts/                          ← כל העיבוד והבדיקות
    │   ├── build.py                      ← raw → processed
    │   ├── checks.py                     ← ★ 29 בדיקות. exit 1 עוצר
    │   ├── crosscheck_baseline.py
    │   ├── bundle_standalone.py
    │   ├── test_ui_menu.js               ← 428 בדיקות Playwright
    │   ├── test_exif.js
    │   ├── check_network.py              ← מה באמת נגיש דרך ה-proxy
    │   ├── build_constraints.py          ← REN/RAN לפי CAOP 2025
    │   ├── build_floods.py               ← שכבת השיטפונות של APA
    │   ├── fetch_*.py  import_*.py       ← משיכה וקליטה לפי מקור
    │   └── ingest_*.py
    ├── docs/
    │   ├── ARCHITECTURE.md               ← ★ מקור האמת על המבנה
    │   ├── HANDOFF.md                    ← המסמך הזה
    │   ├── DATA-ACQUIRED.md              ← מה נמשך בפועל
    │   ├── DATA-REQUEST.md
    │   ├── NETWORK-ALLOWLIST.md          ← מה שנשאר לפתוח
    │   ├── WORKPLAN.md
    │   ├── DELIVERY.md
    │   └── DESIGN.html
    └── android/
        ├── SIGNING.md                    ← ★ פער החתימה. ראה §6
        ├── RELEASE_NOTES.md              ← גוף ה-release בגיטהאב
        ├── build.gradle
        └── app/
            ├── build.gradle              ← קורא את VERSION
            └── src/main/
                ├── AndroidManifest.xml   ← בדיקה 7v מפרסרת אותו
                ├── res/values/strings.xml
                └── java/app/porto/atlas/
                    └── MainActivity.java ← המעטפת. שלושת הגשרים
```

**ענף העבודה:** `claude/mobile-app-pdf-knowledge-aoomsp`.
‏**ה-workflow בונה APK רק מהענף הזה.** דחיפה לענף אחר לא תייצר קובץ התקנה.

---

## 3. כללי הברזל — לא ניתנים למשא ומתן

מלאים ב-`CLAUDE.md` וב-`ARCHITECTURE.md` §2. בתמצית:

1. **לכל שדה מספרי מקור ושנת ייחוס** ב-`data/sources.json`, ושניהם מוצגים.
   כל מספר על המסך לחיץ ופותח את רשומת המקור שלו.
2. **אין ערכי מילוי.** שדה חסר = ״אין נתון״. לעולם לא 0, לא הערכה, ולא
   אינטרפולציה שלא סומנה.
3. **״מאומת״ דורש מקור שני עצמאי.** אחרת `reported`, ולכל דבר נגזר `approx`.
4. **אין ניסוח מטעה.** `crimes registados por 1000 habitantes` אינו ״פשיעה
   חמורה״. ‏`perigosidade` אינה `risco`. **אל תחזק ניסוח של מקור.**
5. **‏OpenStreetMap הוא ODbL** ומחייב `© OpenStreetMap contributors`.
6. **תעתיק עברי לפי הגייה פורטוגזית**, ותעתיקים קיימים **לא** נוצרים מחדש.
7. **כשמקור דו-משמעי — לא מפרסמים, ורושמים למה** תחת
   `sources.json → missing.items`. **הרשימה הזאת היא חלק מהמוצר.**

ועוד שניים שנלמדו בדרך הקשה:

8. **`data/processed/` נוצר.** לעולם לא עורכים ביד — הבנייה הבאה תמחק.
9. **`porto/docs/ARCHITECTURE.md` מתעדכן באותו commit** ששינה את מה שהוא מתאר.

---

## 4. לולאת הפיתוח — הפקודות שעובדות היום

```bash
cd "$PORTO"                           # /home/user/book-gage/porto

# 1. עריכה: scripts/*.py או app.js או app.css או index.html

python3 scripts/build.py              # data/raw → data/processed
python3 scripts/checks.py             # ★ exit 1 עוצר הכול
python3 scripts/crosscheck_baseline.py
node --check app.js
node scripts/test_exif.js

# בדיקות הדפדפן צריכות origin אמיתי — לא file://
python3 -m http.server 8234 &
URL=http://127.0.0.1:8234/index.html node scripts/test_ui_menu.js   # 428 בדיקות

python3 scripts/bundle_standalone.py  # → porto-standalone.html
```

**מעלים גרסה** ב-`VERSION` — נקרא גם ב-`build.py` וגם ב-
`android/app/build.gradle`. **דחיפה לענף מפעילה את בניית ה-APK אוטומטית.**

**ספריות שהבנייה דורשת:** `shapely`, `pyproj`, `pyshp`, `openpyxl`, `sqlite3`,
`geographiclib` — ובנוסף `playwright` כמודול **node**, לחבילת הדפדפן.
**אין GDAL ואין geopandas.** ‏GeoPackage נקרא כ-SQLite + `shapely.wkb`.

> **‏2026-09-14: מכולה חדשה לא מגיעה עם אף אחת מהן.** ‏`build.py` נופל מיד על
> ‏`ModuleNotFoundError: geographiclib`. הרשימה שהייתה כאן תיארה מכולה
> מסוימת, לא את המאגר. **הפקודה הראשונה בסשן חדש:**
>
> ```bash
> bash scripts/setup_env.sh
> ```
>
> ‏`requirements.txt` הוא ההצהרה, והסקריפט מוסיף את מה ש-pip אינו יכול לתת:
> ‏`playwright` כמודול **node**, שהוא דבר אחר מחבילת הפייתון באותו שם. הוא גם
> **בודק ש-Chromium במקומו ונופל אם לא** — אחרת חוסר הדפדפן מתגלה 428 בדיקות
> מאוחר יותר כ-timeout. **הוא לעולם לא מריץ `playwright install`.**

‏Chromium ל-Playwright: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
‏(`--no-sandbox` נדרש). **אל תריץ `playwright install`.**

---

## 5. מנגנוני האימות — ומה הנוהג סביבם

### ‏`scripts/checks.py` — 29 בדיקות

מזוהות לפי אות: ‏`1`–`7`, ואז `7b`…`7w`. הרשימה המלאה עם ההסבר לכל אחת
נמצאת ב-`ARCHITECTURE.md` §8. הן מכסות: שלמות מקורות, קודי DICOFRE,
התאמת אוכלוסייה, תרגום (כל מחרוזת עברית שמגיעה למסך חייבת אנגלית),
‏REN/RAN, תקינות ה-XML של המניפסט, וההסכם בין העמוד למעטפת.

### שתי חבילות בדיקה בדפדפן

| קובץ | מה | כמה |
|---|---|---|
| `scripts/test_ui_menu.js` | פריסה, תפריט, גבולות, נ.צ., תמונות, שפה, מגבלות בנייה | 428 |
| `scripts/test_exif.js` | קורא ה-EXIF, כולל מקרה ה-GPS הריק | — |

### ★ הנוהג: בדיקה חדשה חייבת להוכיח שהיא יכולה ליפול

**זה כלל, לא המלצה.** לפני שמוסיפים בדיקה — שוברים את הדבר שהיא אמורה
לתפוס ומוודאים שהיא נופלת. כל כלל ב-`checks.py` נוסף אחרי שמשהו נשבר באמת,
ו-§11 מתעד מה.

**ואיך שוברים:** על **עותק**, לא על הקובץ החי. תיקיית symlinks עם קובץ אחד
אמיתי ומשוכתב, שרת על פורט אחר, והמקור לא נגעו בו:

```bash
D=/tmp/bugtest; rm -rf $D; mkdir -p $D
for f in "$PORTO"/*; do ln -s "$f" "$D/$(basename "$f")"; done
rm $D/app.js                       # ואז כותבים גרסה משוכתבת במקומו
cd $D && python3 -m http.server 8235 &
cd "$PORTO"
URL=http://127.0.0.1:8235/index.html node scripts/test_ui_menu.js
```

שינוי-במקום ברקע תוך כדי עריכה של אותו קובץ **הרס עריכות** בפרויקט הזה
(‏§11). אל תעשה את זה.

---

## 6. אנדרואיד

### הבנייה והפרסום

`.github/workflows/porto-android-apk.yml` נבנה על דחיפה לענף העבודה שנוגעת
באחד מאלה **בלבד** — הרשימה מדויקת, ולא `data/**`:

```
porto/android/**          porto/data/processed/**
porto/index.html          porto/data/sources.json
porto/app.js              porto/data/prose_en.json
porto/app.css             porto/data/layers_manifest.json
.github/workflows/porto-android-apk.yml
```

**שינוי ב-`data/raw/` או ב-`docs/` לבדו לא יפעיל בנייה.** אם צריך APK בכל
זאת — להעלות `VERSION` (שנקרא ב-`build.py`, כלומר `data/processed/` משתנה),
או `workflow_dispatch` ידני.

הבנייה אורכת כדקה ומפרסמת ל-release בתג `porto-atlas`:

- `portoland-latest.apk` — **קישור קבוע, תמיד האחרונה**
- `portoland-<version>.apk` — אותו קובץ עם מספר
- `porto-atlas.apk` — שריד לקישורים ישנים

```
https://github.com/lior77/book-gage/releases/download/porto-atlas/portoland-latest.apk
```

### ⚠️ אין מפתח חתימה — וזה כואב בכל שחרור

בלי הסודות `ANDROID_KEYSTORE_B64` ו-`ANDROID_KEYSTORE_PASSWORD`, כל בנייה
נחתמת במפתח debug אחר, ואנדרואיד **לא יתקין עדכון על גבי הישן**. כל שחרור
דורש הסרה והתקנה, **וההסרה מוחקת את המקומות, התמונות והשכבות**.

`porto/android/SIGNING.md` מסביר את התיקון — חמש דקות, פעם אחת.
**המפתח הפרטי לא עובר דרך שום צ׳אט ולא נכנס למאגר.** זה נאמר במפורש שם.

**זה הפריט הפתוח בעל ההשפעה הגדולה ביותר על המשתמש.**

### שלושת הגשרים אל המעטפת

| גשר | מה | למה בכלל |
|---|---|---|
| `PortoSave.save(name, b64)` | כותב קובץ ל-Downloads | אין download manager מאחורי WebView; ‏`<a download>` נבלע בשקט |
| `PortoPick.open/take/trail` | בחירת קובץ | ראה §7 — זה הסיפור הגדול |
| `window.__portoBack()` | כפתור החזרה של המערכת | מטפס ברמות של האפליקציה לפני שהוא יוצא |

וגם: `shouldInterceptRequest` מגיש את האפליקציה מ-`https://porto.local/` —
‏origin אמיתי, שבלעדיו אין הרשאת מיקום ואין localStorage.

---

## 7. בחירת תמונה — אושר ב-1.41.4

**התיקון עובד. המשתמש אישר ב-2026-09-14** — הציטוט המדויק ותחומו למטה.
הסעיף נשאר כאן כי הוא מתעד באג ששלושה תיקונים נכונים לא תפסו, ואת הדרך שבה
נמצא. **מה שעדיין לא נבדק נמצא בסופו.**

### מה דווח

״לחיצה על מתמונה פותחת בוחר, אחרי בחירת התמונה חוזרים למסך ׳מקור מקום חדש׳
בלי אפשרות להזנת פרטים ושמירה״ — ובגרסה מאוחרת יותר: **״ולא מוצגת שום הודעה״.**

### שלושה תיקונים שכוונו לשכבה הלא נכונה

| גרסה | מה תוקן | האם זה היה הבאג |
|---|---|---|
| 1.41.1 | שדות הקובץ הוצאו מ-HTML שנבנה מחדש | באג אמיתי. **לא זה** |
| 1.41.2 | `singleTask` → `singleTop` במניפסט | באג אמיתי. **לא זה** |
| 1.41.2 | נוסף דיווח על מה שהבוחר עשה | **הדיווח עצמו שתק במקרה הנחקר** |

שלושתם טיפלו באובייקטים **בזיכרון**.

### מה באמת קורה

בזמן שבוחר התמונות של המערכת בחזית, **אנדרואיד רשאי להשמיד את ה-Activity**.
האפליקציה הזאת שמנה במיוחד: כל הגאומטריה של המחוז ועד 21.7MB תיחומי REN/RAN
יושבים ב-heap של ה-WebView, והבוחר אינו תוכנית קטנה. כשזה קורה נעלם הכול —
ה-`ValueCallback`, השדה, ומצב העמוד. ‏`onCreate` מריץ `restoreState()` ומצייר
**בדיוק את המסך שהקורא היה בו**, והתוצאה מגיעה ל-Activity שאין לו למי למסור.
ולכן גם אין הודעה: **מי שהיה אמור לדבר כבר לא קיים.**

### התיקון ב-1.41.4

שום דבר במסלול אינו יושב בזיכרון:

| שלב | איפה |
|---|---|
| הבקשה | `SharedPreferences → pick-kind`, **לפני** פתיחת הבוחר |
| הבתים | `cacheDir/picked/` — קובץ של האפליקציה, לא `content://` של מופע |
| התשובה | `SharedPreferences → pick-result` |
| המסירה | **העמוד בא ואוסף**: `PortoPick.take()` |

העמוד אוסף בשלושה מועדים, וכל אחד מכסה מקרה שהאחרים מפספסים:
`window.__portoPicked` (דחיפה), `pageshow`, ו-`visibilitychange` — **האחרון
הוא המקרה שהיה שקט.** הבתים מוגשים מ-`https://porto.local/__picked/<name>`,
כך ששום תמונה אינה חוצה את גשר ה-JavaScript כ-base64.

**ושום תוצאה אינה שקטה** — ביטול כלול. בוחר שבלע תמונה עונה `RESULT_CANCELED`,
כלומר הכישלון **אינו ניתן להבחנה** מביטול; השתקת הביטול השתיקה בדיוק את מה
שחיפשנו. תוצאה שלא הגיעה כלל נאמרת גם היא (`lost:<kind>`, אחרי 1.5 שניות
ב-`onResume`).

### מה אושר — ומה בדיוק

**‏2026-09-14, מהמשתמש: ״הזרימה אחרי בחירת התמונה תקינה״.**

זה סוגר את **מתמונה**: הטופס נפתח עם התמונה, כלומר האיסוף עשה את שלו והתשובה
שרדה את השמדת ה-Activity. **זה, ורק זה, אושר** — המשפט נוקב בזרימה שאחרי
בחירת תמונה ואינו אומר דבר על שני הנתיבים האחרים. אל תחזק אותו מעבר לכך.

`PortoPick.trail()` נשאר לא חשוף ב-UI, ואין סיבה לחשוף אותו כל עוד הנתיב
עובד. **אם יחזור דיווח על שתיקה — הוא עדיין הדבר הראשון לחשוף.**

### שני הנתיבים האחרים — ושם נמצא באג

המשתמש אמר במפורש: **״לגבי שני הכפתורים הנוספים לא בדקתי תקינות הזרימה״** —
כלומר **ממפה** ו**מכתובת**. **שניהם אינם נוגעים בבוחר של אנדרואיד בכלל:**
״ממפה״ פותח מצב הצבה על המפה, ו״מכתובת״ מחפש בגָזֵטִיר של האפליקציה עצמה.
לכן האישור על ״מתמונה״ לא אמר עליהם דבר — זה קוד אחר לגמרי.

**נבדקו ב-2026-09-14.** ״ממפה״ כבר היה מכוסה מקצה לקצה. **״מכתובת״ היה שבור
לחלוטין**, ואיתו **גם החיפוש הראשי של האפליקציה** — ראה §12 מלכודת 1.

---

## 8. חומר חיצוני — SharePoint: `GPT/` ו-`גדול/`

**‏https://lioravital.sharepoint.com/sites/lioravital/DocLib1/Portoland**

> **‏★ הסעיף הזה הוא היסטוריה. המצב העדכני נמצא ב-`data/source_files/INDEX.md`**,
> שבו נכתב ב-2026-09-14 מה קרה **לכל פריט** בשתי התיקיות: במאגר, ניתן להורדה
> מחדש (עם הכתובת שנבדקה חיה), או ייאבד. **מחליטים על מחיקה משם, לא מכאן.**

שתי תיקיות שם החזיקו חומר שחלקו לא היה במאגר. נגישות דרך מחבר Microsoft 365
(`sharepoint_folder_search` ואז `read_resource`). נבדקו פריט-פריט ב-2026-09-14,
**ו-19 קבצים (‏כ-360KB) יובאו למאגר** תחת `data/source_files/{dgt,apa,icnf,pdm}/`.

> **שתי מגבלות של המחבר שקובעות מה אפשר להעביר בכלל:**
> **‏(1) כל בינארי נדחה** — `application/zip`, `application/octet-stream`.
> ‏zip, gpkg **וגם ה-notebooks** אינם ניתנים להעברה דרכו.
> **‏(2) קריאה גדולה נחתכת ב-200,002 תווים** — כך התקבל
> ‏`ine_0012234_metadata_raw.json` **חתוך ולא תקין**. הוא נמחק והורד במלואו
> מ-INE. **קובץ ראיה חתוך גרוע מכלום.**

### ‏`GPT/` — ‏כ-160MB. העבודה שקדמה למאגר

| מה | מצב מול המאגר |
|---|---|
| ‏INE מחירים ושכירות (`ine_precos_venda.csv` 595KB) | **הוטמע ועודכן.** במאגר `data/raw/ine/ine_precos_venda.csv` עם **72,618 שורות** מול 9,282; אותה תקופה, עם `sha256` ו-`fetch_report.json`. גרסת המאגר מחליפה |
| `REN_FINAL/` ‏(19.9MB), `RAN_RAW/`, `dgadr_ran.gpkg` ‏(15.4MB) | **התוצר הוטמע, הגלם לא.** במאגר `data/raw/constraints_caop2025.json` ו-`data/layers/`, מאומת מול `area_ha` של DGT עד 0.0032%. ה-gpkg עצמם אינם במאגר |
| `apa_zonas_inundaveis.gpkg` ‏(37.7MB, **157 מצולעים**, EPSG:3763) | **לא הוטמע.** במאגר רק שלושה zip של ה-`limite` ‏(4.7MB), ו**האפליקציה אינה משתמשת בשיטפונות כלל** — `layers_manifest.json` מכיל `ren` ו-`ran` בלבד |
| `icnf_perigosidade_*` (מטא-דאטה ו-WFS capabilities) | **המסקנה הוטמעה, הראיה לא.** ‏`missing.items` מתעד למה סכנת שריפה אינה באפליקציה; קובצי הראיה רק שם |
| `sources_ren.json` ‏(106KB), `sources_ran.json` ‏(30KB), `ren_download_manifest.json` ‏(62KB), `checksums.sha256` | **לא הוטמע.** תיעוד מקור ברזולוציה של מאפיין בודד. כל `data/sources.json` של האפליקציה הוא 80KB |
| `PROJECT_HANDOFF_PORTO_REAL_ESTATE.md` | הכללים 1–4, 9, 10 שבו הם במהותם חוזה הדיוק. **ארבעה לא עברו** — ראה למטה |
| `ine_raw_quarters/` ‏(17.3MB) | גלם רבעוני. לא במאגר |

#### ‏`GPT/PDM_RAW/` — ‏114KB, ו**זה רק הרישום**

הנקודה הכי קלה לטעות בה: התיקייה מחזיקה `pdm_source_inventory.json`,
`pdm_phase1_vector_services.json`, `pdm_legal_status_updates_2026-09-08.json`,
שני `PDM_PHASE1_STATUS*.md`, ארבעה notebooks ו-README. **אין בה נתונים
וקטוריים.**

הנתונים שהיא מתעדת יושבים ב-**Google Drive** (המסמך כותב
`/Google Drive/עבור got/PDM_RAW`, ו-`PDM_PHASE1_STATUS_AFTER_RUN.md` אומר
״Drive size observed״):

```
Matosinhos                 89/89   ok
Paredes                   101/101  ok
Valongo — Ordenamento       24/24   ok
Valongo — Condicionantes    30/30   ok
                          ───────
                          244/244
Porto   porto_carta_qualificacao_solo_pdm2021.gpkg   139,554,816 bytes
Póvoa de Varzim  — timeout. מסומן **לנסות שוב**, לא ״לא זמין״
```

**‏`porto_pdm_claude/` שבשורש המאגר מכסה עיריות אחרות לגמרי:** ‏Maia
ו-Gondomar (וקטור מאומת, `VERIFIED_VECTOR`), ‏Gaia ו-Felgueiras
(`UNRESOLVED` — השרתים לא נגישים מהסביבה). **אפס חפיפה.** יחד: 6–7 מתוך 18.

### ‏`גדול/` — ‏כ-800MB. קובצי המקור הלאומיים

| קובץ | גודל | מצב |
|---|---|---|
| `portugal2021.zip` | 258.6MB | מפקד 2021 מלא |
| `CAOP_Continente_2025-gpkg.zip` | 111.6MB | **‏CAOP 2025 הארצי.** במאגר יש את חתך מחוז 13 בלבד |
| `C2021_SECCOES_PT.zip` | 67.6MB | מקטעי מפקד |
| `LUGARES21_PORTUGAL.zip` | 61.6MB | יישובים |
| `C21_LUGF_PT.zip` + תיקייה | 60.8MB + 122MB | |
| `Portugal_Property_Baseline_v2.gpkg` / `.csv` | 55.3MB / 1.1MB | **ה-CSV כן במאגר** (‏`data/source_files/ine/`) ו**בשימוש פעיל** כמקור אימות שני ב-`crosscheck_baseline.py`. ה-gpkg לא |
| `FS2021SubSeccaoTot.zip` | 44.5MB | תת-מקטעים |
| `FS2021SeccaoTot.zip` | 13.4MB | |
| `BGRID1K21_PORTUGAL.zip` | 11.3MB | רשת אוכלוסייה 1 ק״מ |
| `C2021_FSINTESE_VARIAVEIS*.txt/.pdf` | 78KB | **כבר במאגר, בשם אחר** — `FS2021_Seccao_variaveis.txt/.pdf` ו-`GRID1K21_variaveis.txt`. אומת ב-2026-09-14 **בתוכן ולא רק בגודל**: זהים |
| `BGRI2021_to_CAOP2025_reassignment.csv` | 7.9KB | **הקובץ עצמו במאגר**, ‏`data/source_files/ine/`, זהה, 330 שורות |
| `C21_LUGF_PT.zip` + תיקייה | 60.8MB + 122MB | **★ הפער לכיוון ההפוך.** ‏`data/source_files/INDEX.md` כותב ״קובץ הנתונים עצמו עדיין לא הגיע — ההעלאה נכשלה״, **והוא כאן.** במאגר רק מילון המשתנים שלו |

**מחוז 13 כבר נגזר מכאן ונמצא במאגר** (‏`raw/dgt/caop_freguesias.geojson`
‏275 רובעים, `raw/bgri2021_to_caop2025_d13.json` ‏21,337 תת-מקטעים,
‏0 לא משויכים), **ומילון המשתנים כבר במאגר גם הוא.** מה שנשאר שם הוא
**הכיסוי הארצי** — נחוץ רק אם יורחב מעבר למחוז — **ו-`C21_LUGF_PT`, שהמאגר
מתעד כחסר.**

### ★ ממצא דיוק שעולה מההשוואה

‏`GPT/PROJECT_HANDOFF_PORTO_REAL_ESTATE.md` מנסח כך:

> Porto and Vila do Conde returned zero features from the official WFS
> **at download time; this is not proof that no legal REN exists.**

‏`data/sources.json` אמר: **״אין REN ואין RAN לפורטו, ואין REN לווילה דו
קונדה״**. זו טענה **חזקה יותר ממה שהראיה מאפשרת** — מרשם שהחזיר אפס
מאפיינים ביום מסוים אינו היעדר תיחום חוקי. **זה בדיוק כלל הברזל הרביעי.**

**תוקן ב-2026-09-14.** הניסוח עכשיו אומר מה שקרה: המרשם לא החזיר ולו מצולע
אחד בשליפה שממנה נבנה הערך, **וזו היעדרות מהמרשם ביום שנקרא ולא ראיה שאין
תיחום חוקי.** התיקון נגע בחמישה מקומות, לא באחד — `caveat_he` של
‏`map.ren_ran`, אותו משפט ב-`cons_pct` של עירייה ושל רובע, והמחרוזות
המקבילות ב-`prose_en.json`; ובקוד, ה-docstrings של `build_constraints.py`
ו-`build_layers.py`. **המסך עצמו כבר היה נכון** — הוא מציג ״אין תיחום
מפורסם״, ולא ״אין תיחום״.

### ארבעה כללים מה-GPT שאינם ב-`CLAUDE.md`

1. העדפת **EPSG:3763**; ‏4326 רק אם זה ה-CRS המקורי, ולא להמיר בלי ודאות.
2. העדפת **GeoPackage** לתוצרי GIS.
3. **‏SHA-256 לכל קובץ מקור.** במאגר יש את זה ב-`fetch_report.json`, לא כנוהג.
4. **רק מסמכי תכנון שנכנסו לתוקף** — טיוטות ורוויזיות תלויות מוחרגות.
   ולצידו: **לשאול לפני איסוף שכבת PDM מחוץ לליבה המאושרת** (סיווג
   וייעוד קרקע בלבד).

### מה לעשות עם זה

**המצב אחרי הייבוא של 2026-09-14** — הפירוט המלא ב-`INDEX.md`:

| | |
|---|---|
| **‏`GPT/`** | הכול במאגר או אומת כניתן להורדה מחדש, **פרט לארבעת ה-notebooks של PDM** ‏(79KB) שהמחבר דוחה ואין להם מקור אחר |
| **‏`גדול/`** | 6 מתוך 9 אומתו כניתנים להורדה מחדש **בגודל מדויק**. לא אומתו: `Portugal_Property_Baseline_v2.gpkg` (תוצר נגזר), `portugal2021.zip` ו-`C2021_SECCOES_PT.zip` |
| **‏`RAN_RAW`** | ‏26 ה-SHA-256 במאגר, עמוד DGADR חי — אבל ההורדה שם עוברת טופס, כלומר ידנית |

**מה שכבר נעשה, ואין צורך לחזור עליו:**

1. **רישום ה-PDM מוזג.** ‏`data/source_files/pdm/pdm_register_merged.json` —
   ‏18 עיריות, מהן 4 עם בדיקה עמוקה מ-`porto_pdm_claude/` ‏(2 `VERIFIED_VECTOR`),
   ‏5 עם גרסה חוקית מאומתת ו-5 עם מצב איסוף. **המיזוג אישר אפס חפיפה.**
2. **קובצי ה-`sources_*.json` במאגר** — והראיה כבר הועילה: ההסבר של DGADR
   ואזהרת DGT נכנסו ל-`sources.json` ומוצגים באפליקציה.
3. **‏`C21_LUGF_PT` אותר ואומת** — ‏60,842,705 בתים ב-`mapas.ine.pt`, בדיוק
   כגודל העותק ב-״גדול״. השורה ב-`INDEX.md` שטענה שההעלאה נכשלה תוקנה.
   **לא הוכנס ל-git** — 61MB של כיסוי ארצי כשצריך מחוז 13.

> **‏2026-09-14: הטבלאות למעלה תוקנו מול התוכן בפועל.** שלוש שורות היו שגויות
> — מילון המשתנים, טבלת ההמרה ו-`Portugal_Property_Baseline_v2` כבר במאגר.
> ההמלצה השלישית כאן הייתה ״להעביר את מילון המשתנים״, וזה כבר נעשה מזמן.
> **אודיט שנכתב מרשימת קבצים ולא מהצלבה מול המאגר יטעה בדיוק כך:** שם הקובץ
> במאגר אינו שם הקובץ במקור.

**ה-gpkg וה-zip הגדולים לא נכנסים ל-git.** הם גלם. המקום לרשום שהם קיימים,
איפה, ומה ה-md5 שלהם הוא `data/source_files/INDEX.md`.

---

## 9. עבודה פתוחה, לפי סדר השפעה

| # | מה | מצב |
|---|---|---|
| 1 | **מפתח חתימה** (`porto/android/SIGNING.md`) | ממתין למשתמש. כל שחרור מוחק נתונים בלעדיו |
| 2 | **‏PDM — למזג את שני החצאים** | ‏§8. ‏`porto_pdm_claude/` ו-`GPT/PDM_RAW` מכסים עיריות שונות ואפס חפיפה |
| 3 | **‏CRUS** (שימושי קרקע, ~2.5MB לעירייה) | הוצע, לא נבחר. האוסף `crus` חי ב-OGC API — נבדק ב-2026-09-14 |
| 4 | **קישורי תקנון PDM** | הוצע, לא נבחר |

### נסגר — נשאר רשום, כי ״נבדק והושלם״ שווה כמו ״נבדק ונדחה״

| מה | הראיה |
|---|---|
| **קליטת CAOP 2025** | ‏`raw/dgt/caop_freguesias.geojson` = **275 רובעים**, ‏`caop_municipios` = 18; `sources.json` מצטט ״CAOP 2025״. הערות ה-243 שנשארו ב-`build.py` הן שאריות טקסט, לא המצב |
| **אוכלוסייה ל-57 הרובעים החדשים** | הבנייה מדפיסה **275/275** עם אוכלוסייה, שם עברי ושטח. ‏`raw/bgri2021_to_caop2025_d13.json`: ‏21,337 תת-מקטעים, **0 לא משויכים**, 275 סכומי רובע |
| **תיקון בחירת התמונה (1.41.4)** | המשתמש, 2026-09-14: **״הזרימה אחרי בחירת התמונה תקינה״**. ‏§7. ‏**״מתמונה״ בלבד** — ממפה ומכתובת נשארו פתוחים |
| **ניסוח היעדר ה-REN** | ‏§8. תוקן ב-2026-09-14 בחמישה מקומות. בדיקה 7n מאמתת שלכל מחרוזת שתוקנה יש אנגלית — הוכח על עותק שהיא נופלת בלעדיה |
| **שכבת השיטפונות** | נוספה ב-1.44.0 לבקשת המשתמש. ‏APA ממפה **23 אזורי מחקר בארץ** ואחד מהם ״פורטו״ — כלומר **3 מתוך 18 עיריות**: פורטו 6.4%, גאיה 1.8%, גונדומאר 0.002%. ‏15 בלי ולו מצולע אחד, ו**האפליקציה אומרת זאת** במקום להציג מפה נקייה. ‏15 בדיקות, הוכחו נופלות מול הקוד שלפני |
| **שני הנתיבים שלא נבדקו, ושני החיפושים** | ‏§7 ו-§12 מלכודת 1. ״ממפה״ היה תקין ומכוסה. ״מכתובת״ **והחיפוש הראשי היו מתים מ-1.32.0** — תוקן ב-2026-09-14. ‏15 בדיקות חדשות, והוכח שהן נופלות מול הקוד הישן: ‏12 כשלים |
| **צמצום קו הרובעים ברמה 3** | **לא היה פתוח מעולם.** נמדד בדפדפן ב-2026-09-14: ברמה 3 מצויר רובע אחד והוא השחור; ברמה 2 ‏26 רובעים ועירייה אחת מ-18. הצמצום קורה ב-`freHere`, לא ב-`ownFeature()`, וענף ל-`zone` היה קוד מת. ‏8 בדיקות חדשות נועלות את שלוש הרמות |

> ‏`porto/docs/ARCHITECTURE.md` §12 עדיין מונה את **שתי הראשונות** כחסרות
> (‏CAOP 2025 והאוכלוסייה). **הוא מיושן בשתי השורות האלה** — תוקן כאן
> ב-2026-09-14 לפי בדיקה בקבצים.

`data/sources.json → missing.items` מחזיק **4 פריטים** שנחקרו ונדחו במפורש
(מעלית, מחיר ברמת הרובע בתשע עיריות, גבול שכונה, שנת מפת השריפות).
‏`porto/docs/ARCHITECTURE.md` §12 מסביר כל אחד. **אלה לא משימות פתוחות — אלה החלטות.**

---

## 10. מוסכמות בקוד

### שפה

- **עברית היא שפת המקור.** ‏`t()` היא זהות בעברית ומחפשת בטבלה באנגלית.
- הטבלה: `Object.assign(EN, {...})` **בסוף `app.js`**.
- **בדיקה 7n נופלת** על כל מחרוזת עברית שמגיעה למסך בלי אנגלית.
- ‏7u תופסת מחרוזות שנישאות בטבלה (`t(CONS_HE[k])`) ש-7n אינה רואה.
- שמות מקומות: `nm()` / `nmPair()`. פרוזת נתונים: `prose()`.

### ממשק

- **`confirm()` אסור.** ‏WebView מצייר אותו בשפת המערכת. הדפוס הוא **שתי
  לחיצות** (״לחיצה נוספת תמחק״).
- הודעות: `mapNote(html, isBad)`.
- ‏`PREF_REV` — העלאה מפילה פעם אחת העדפות שנשמרו ב-`DEFAULT_RESET`.
  **ברירת מחדל שמשתנה בקוד בלבד לא משנה דבר בטלפון שכבר מותקן.**

### ‏Git

- הודעת commit מתארת **מה נשבר ולמה**, לא רק מה שונה.
- **אין מזהה מודל** בהודעות commit, ב-PR, בהערות קוד או בכל תוצר במאגר.
- **לא פותחים PR** אלא אם ביקשו במפורש.

---

## 11. הסביבה — ארבע מלכודות שעלו בזמן אמיתי

### ‏`pgrep -f` / `pkill -f` תופס את הפקודה שמריצה אותו

**נקרה ארבע פעמים.** `pkill -f "node scripts/test_ui_menu"` הורג את המעטפת
עצמה (exit 144). ו-`pgrep -f "http.server 8234"` **דיווח ״השרת למעלה״ כששום
שרת לא רץ** — והחבילה נפלה על timeout.

> **בדיקת חיים נעשית מול השירות, לא מול טבלת התהליכים:**
> `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8234/index.html`
> להריגה — לפי PID.

### המכולה קופאת בין תורים

תהליכי רקע מתקדמים בעיקר **בזמן שפקודה רצה**. חבילה של 428 בדיקות יכולה
להיראות תקועה. פתרון: לולאת המתנה **בחזית** (עד 600 שניות), עם `time.sleep`
של פייתון — ‏`sleep` בחזית חסום בהארנס.

### הרשת עוברת דרך proxy

‏CA bundle: `/root/.ccr/ca-bundle.crt`.
**לעולם לא לכבות אימות TLS ולעולם לא לבטל `HTTPS_PROXY`.**
‏`scripts/check_network.py` מדפיס מה באמת נגיש. **לא `curl -I`** — שרתים
פורטוגזיים סוגרים חיבור על HEAD ומגישים GET כרגיל, וזה נקרא בטעות כחסימה
לאורך כל הפרויקט.

### ‏GitHub API מגיש מצב ישן

`actions_list` החזיר `in_progress` ‏**11 דקות** אחרי שהריצה נכשלה. ל-polling
אמיתי: `curl` ישיר ל-`api.github.com`.

---

## 12. שתים-עשרה המלכודות שהכי כדאי להכיר

‏§11 ב-`porto/docs/ARCHITECTURE.md` מחזיק **87**. אלה הכי יקרות:

1. **משתנה מקומי בשם `t` שהצל על פונקציית התרגום `t()`** — ‏`const t =
   term.trim().toLowerCase()` יושב ב-`placeHits()` וב-`runSearch()` מאז
   הבנייה המקורית; ‏1.32.0 הוסיף את `t()` הגלובלית, וכל `t('עירייה')` בשתי
   הפונקציות האלה הפך לקריאה למחרוזת. **שני החיפושים מתו בשקט תשע גרסאות.**
   שלוש שכבות הגנה לא ראו את זה: החבילה לא נגעה במסך החיפוש, ‏7n בודקת
   מחרוזות חסרות ולא הצללה, ו-`node --check` עובר — הקוד תקין תחבירית.
   **וגם הכישלון הסתתר:** החיפוש הראשי זרק לפני שצייר משהו, אבל ״מכתובת״ זרק
   **רק כשהיה מה להציג** — שאילתה בלי תוצאות חזרה נקייה. הנתיב שעבד היה הריק,
   והוא הנתיב שבדיקה מהירה נוטה לקחת. **מאז החבילה מאזינה ל-`pageerror`
   ונופלת על כל שגיאה לא נתפסת**, וזו ההגנה שהייתה חסרה.
2. **מפה ריקה היא הדבר הבטוח ביותר שמפה יכולה לומר** — שכבת השיטפונות מכסה
   ‏3 מתוך 18 עיריות, כי APA ממפה 23 אזורי מחקר בלבד. אמרנטה ובייאו יושבות על
   הטמגה ואינן מכוסות. מי שמדליק את השכבה שם רואה **כלום** — וכלום נקרא ״אין
   סכנה״. **התשובה אינה להסתיר את השכבה אלא לדבר:** `floodNote()` אומר שלא
   מופתה, והספירה ״3/18״ יושבת על המתג לפני שנוגעים בו. **לפונקציה ניתן שם
   בדיוק כדי שאפשר יהיה לבדוק אותה** — התנהגות בלי שם אינה נבדקת.
3. **‏Activity שנהרס בזמן שהבוחר בחזית** — §7 כאן. שני תיקונים נכונים לשני
   באגים אמיתיים לא הזיזו את התסמין.
4. **מכשיר ששותק בדיוק במקרה הנחקר** — ביטול הושתק ״כי הקורא יודע״. הכישלון
   נראה בדיוק כמו ביטול. **מכשיר לא בוחר אילו תוצאות מעניינות.**
5. **מה שעובד לעירייה אחת קורס בשמונה-עשרה** — ‏`L.geoJSON` על 1,556,351
   קודקודים הוא 200MB+ heap. בטלפון זו קריסה, בלי הודעה. התשובה:
   `Float64Array` מוטל מראש וציור לקנבס. **״זה עובד״ בדוגמה אחת אינו מדידה.**
6. **מאזין מואצל על HTML שנבנה מחדש** — עובד בדפדפן תמיד, נשבר בטלפון.
7. **קיצור `font` שנגמר ב-`inherit` נזרק כולו** — ‏CSS פסול לא זורק שגיאה
   ולא משאיר סימן. ‏longhand כשצריך לרשת משפחה.
8. **הערה בתוך תג פתיחה של XML** — ‏`Error parsing` בלי שורה, 11 דקות אחרי
   הדחיפה. בדיקה 7v מפרסרת את המניפסט מאז.
9. **‏Leaflet בונה מחדש את `<defs>` בכל ציור רמה** — ‏`<pattern>` חייב להיווצר
   **אחרי** הוספת השכבה, אחרת נזרק בלי שגיאה.
10. **״שטח מוגבל״ הם שני מספרים שאסור לחבר** — ‏REN ו-RAN חופפים עד 16.2%.
   שורת ״סך הכול״ היא האיחוד.
11. **‏GEOS `Unable to determine overlay result geometry dimension`** — התיקון
   הוא `grid_size=0.001` על פעולות ה-overlay עצמן. ‏pre-snapping **לא** עוזר.
12. **״שבור-בדוק-שחזר״ ברקע על קובץ שממשיכים לערוך** — העריכות נכתבות מעל
    הגרסה השבורה ואז נדרסות. **המוטציה על עותק.** §5 כאן.

---

## 13. איך לדעת שלא שברת כלום

```bash
cd "$PORTO"
python3 scripts/build.py && python3 scripts/checks.py \
  && python3 scripts/crosscheck_baseline.py \
  && node --check app.js && node scripts/test_exif.js \
  && python3 scripts/bundle_standalone.py
```

ואז חבילת הדפדפן מול שרת חי. **`ALL CHECKS PASSED (0 warnings)` ו-
`0 failed` — שניהם, אחרת לא דוחפים.**

ואחרי הדחיפה: לחכות לבנייה, **להוריד את ה-APK שפורסם ולוודא בתוכו** שמה
שהתכוונת לשלוח באמת שם. זה תפס מניפסט פגום ששבר בנייה שלמה.

---

## 14. תחזוקת המסמך הזה

עדכן אותו כשמשתנה **מצב**, לא כשמשתנה קוד — לקוד יש את
`porto/docs/ARCHITECTURE.md`.
כלומר: גרסה, מה נסגר, מה נפתח, ומה נלמד שעולה זמן לחזור עליו.

אם פריט ב-§8 נסגר — הזז אותו, אל תמחק. הידיעה ש**נבדק ונדחה** שווה בדיוק
כמו הידיעה שנעשה. זה אותו היגיון של `missing.items`.
